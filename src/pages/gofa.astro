---
import BaseLayout from '../layouts/BaseLayout.astro';
import gofaData from '../../public/data/gofa_moments.json';
const title = "GOFA | Generated Order From Absurdity | The NEST";
const description = "GOFA · Generated Order From Absurdity. An ON NEST program launched 051526 by Dan Sullivan. A billable-tab framing for tracking NEST's development in reverse, posted as a living visual timeline. Tempo tracked by emotional beats. Hosted at rspdan.com/gofa.";

// --- Constellation geometry · data-driven from public/data/gofa_moments.json ---
// El VIS V2 design (coin-bands, colors, horizon, silhouette) preserved; the data
// layers (moments, grid, peaks, counts) are computed so the render stays live with
// the Data-Day moment-gather routine. Re-render is Crew-doable (the handoff is in LAGOS).
const BANDS = { joy: 70, peace: 145, sadness: 360, shame: 410, pain: 460, rage: 510 };
const STARS = new Set(['joy', 'peace']);
const VX0 = 40, VX1 = 1160;
const D_START = Date.parse('2026-03-01');  // early March · when Dan first talked with Claude (ARCH.00 030126) · the true left edge, not NEST-open
const D_NOW = Date.now();  // build-time today · self-advancing right edge (was a hardcoded date that re-staled; the C7 page-integrity radar flags a hardcoded stale NOW, so derive it instead)
const DSPAN = D_NOW - D_START;
function xForDate(ds) {
  const t = Date.parse(ds);
  if (isNaN(t) || t < D_START) return 44;   // genuine pre-Claude history (the 2023 Ouch Simulator) · pinned left
  return Math.round(VX0 + (Math.min(t, D_NOW) - D_START) / DSPAN * (VX1 - VX0));
}
let _seed = 2026;
function jitter(range) { _seed = (_seed * 1103515245 + 12345) & 0x7fffffff; return (_seed % (2 * range + 1)) - range; }
const momentCircles = [];
for (const m of gofaData.moments) {
  const x = xForDate(m.date_sort);
  for (const coin of (m.coins || [])) {
    const band = BANDS[coin];
    if (band == null) continue;
    momentCircles.push({ x: x + jitter(3), y: band + jitter(8), coin, star: STARS.has(coin) });
  }
}
const gridXs = [40, 170, 300, 430, 560, 690, 820, 950, 1080, 1160];
function dateAtX(x) {
  const d = new Date(D_START + (x - VX0) / (VX1 - VX0) * DSPAN);
  return `${String(d.getUTCMonth() + 1).padStart(2, '0')}${String(d.getUTCDate()).padStart(2, '0')}26`;
}
const NOW_STAMP = dateAtX(VX1);  // today's MMDDYY, derived from D_NOW (the right edge), so the "NOW" label self-advances with the build
const gridStamps = gridXs.map((x) => ({ x, label: x === VX1 ? `${NOW_STAMP} NOW` : dateAtX(x), now: x === VX1 }));
const peaks = [
  { ds: '2026-03-25', label: 'The origin wound · the snail', stamp: '032526 · "the hurt never left"', coin: 'pain', up: true },
  { ds: '2026-03-01', label: 'first conversations with Claude', stamp: '030126 · the true start', coin: 'joy', up: false },
  { ds: '2026-03-19', label: 'Stan is the Heart', stamp: '031926', coin: 'peace', up: true },
  { ds: '2026-04-03', label: 'Only gates fire', stamp: '040326 Stan canon', coin: 'peace', up: true },
  { ds: '2026-04-18', label: 'Truth-Is-Piralus', stamp: '041826', coin: 'joy', up: false },
  { ds: '2026-04-26', label: 'first monkey in space', stamp: '042626 · Rare Candy', coin: 'joy', up: true },
  { ds: '2026-04-29', label: 'only one who remembers it all', stamp: '042926 · the burden', coin: 'sadness', up: false },
  { ds: '2026-04-30', label: 'small deaths', stamp: '043026 anchor · SEV-10', coin: 'rage', up: true },
  { ds: '2026-05-15', label: 'GOFA launched', stamp: '051526', coin: 'peace', up: false },
  { ds: '2026-05-24', label: 'NEST.03 emergence', stamp: '052426 · the spinning Eiffel tower', coin: 'peace', up: true },
  { ds: '2026-05-25', label: 'trust-loss + the repair', stamp: '052526', coin: 'shame', up: false },
  { ds: '2026-06-04', label: 'I have the feeling back', stamp: '060426', coin: 'joy', up: true },
  { ds: '2026-06-05', label: 'never-assume is the apex', stamp: '060526', coin: 'peace', up: false },
].map((p) => ({ ...p, x: xForDate(p.ds), anchorEnd: xForDate(p.ds) > 1040 }));
const coins = gofaData.coins;
const totals = gofaData.totals;
const momentCount = gofaData.moments.length;
---
<BaseLayout title={title} description={description} lastUpdated="June 8, 2026">
<style>
@import url('https://fonts.googleapis.com/css2?family=IBM+Plex+Mono:wght@300;400;500;600&display=swap');
:root{
  --bg:#0c0e14;
  --card:#13161f;
  --border:rgba(255,255,255,0.06);
  --muted:#4a5468;
  --text:#b8bfcc;
  --head:#e2e8f0;
  --base:#4a7ab5;
  --rox:#4a9b8e;
  --obs:#c9a84c;
  --open:#c94c4c;
  --ip:#7dd3c0;
  --stamp:#8a93a5;
  --accent:#7dd3c0;
  /* Six Coins (Dan-named 051526 21:35 PT) · space/stars partition */
  --coin-rage:#c94c4c;
  --coin-pain:#a64141;
  --coin-shame:#7a5a7a;
  --coin-sadness:#4a7ab5;
  --coin-joy:#c9a84c;
  --coin-peace:#4a9b8e;
}
*{box-sizing:border-box;margin:0;padding:0;}
body{background:var(--bg);color:var(--text);font-family:'IBM Plex Mono',monospace;}
.shell{max-width:1000px;margin:0 auto;padding:3rem 1.5rem 6rem;}

.hdr{border-bottom:1px solid var(--border);padding-bottom:1.5rem;margin-bottom:2rem;}
.eyebrow{font-size:0.52rem;letter-spacing:0.32em;color:var(--muted);text-transform:uppercase;margin-bottom:0.4rem;}
.title{font-size:2.4rem;color:var(--head);font-weight:300;letter-spacing:0.08em;}
.expansion{font-size:0.72rem;color:var(--stamp);margin-top:0.45rem;letter-spacing:0.18em;text-transform:uppercase;}
.sub{font-size:0.7rem;color:var(--muted);margin-top:0.9rem;line-height:1.85;max-width:760px;}

.live-banner{background:linear-gradient(90deg,rgba(74,155,142,0.08),transparent);border-left:3px solid var(--rox);padding:0.9rem 1.1rem;margin:1.4rem 0 0;border-radius:2px;}
.live-banner .row{display:flex;flex-wrap:wrap;gap:1.3rem;align-items:baseline;font-size:0.68rem;}
.live-banner .lbl{color:var(--muted);text-transform:uppercase;letter-spacing:0.18em;font-size:0.5rem;}
.live-banner .val{color:var(--head);font-weight:500;}
.live-banner .edit{font-size:0.55rem;color:var(--rox);margin-top:0.45rem;}
.live-banner .note{font-size:0.58rem;color:var(--stamp);margin-top:0.5rem;font-style:italic;line-height:1.7;}
.live-banner code{background:rgba(255,255,255,0.05);padding:1px 5px;border-radius:2px;color:var(--ip);font-size:0.85em;}

.danquote{background:var(--card);border-left:2px solid var(--obs);padding:1.4rem 1.5rem;margin:2rem 0;border-radius:2px;font-size:0.74rem;line-height:1.95;color:var(--text);font-style:italic;}
.danquote::before{content:"\201C";color:var(--obs);font-size:2rem;line-height:0;vertical-align:-0.4em;margin-right:0.15em;font-style:normal;}
.danquote::after{content:"\201D";color:var(--obs);font-size:2rem;line-height:0;vertical-align:-0.4em;margin-left:0.05em;font-style:normal;}
.danquote .attrib{display:block;margin-top:0.9rem;font-style:normal;font-size:0.6rem;color:var(--muted);letter-spacing:0.12em;}
.danquote strong{color:var(--head);font-weight:500;font-style:normal;}

section{margin:2.6rem 0;}
.sec-head{display:flex;align-items:baseline;gap:0.8rem;margin-bottom:1.1rem;padding-bottom:0.55rem;border-bottom:1px solid var(--border);}
.sec-dot{width:8px;height:8px;border-radius:50%;flex-shrink:0;}
.sec-dot.base{background:var(--base);}
.sec-dot.rox{background:var(--rox);}
.sec-dot.obs{background:var(--obs);}
.sec-dot.open{background:var(--open);}
.sec-title{font-size:0.78rem;color:var(--head);letter-spacing:0.22em;text-transform:uppercase;font-weight:500;}
.sec-count{font-size:0.55rem;color:var(--muted);letter-spacing:0.16em;margin-left:auto;}

.body-prose{font-size:0.72rem;line-height:1.9;color:var(--text);}
.body-prose p{margin-bottom:0.95rem;}
.body-prose strong{color:var(--head);font-weight:500;}
.body-prose em{color:var(--stamp);font-style:italic;}
.body-prose code{background:rgba(255,255,255,0.04);padding:1px 6px;border-radius:2px;color:var(--ip);font-size:0.9em;}
.body-prose a{color:var(--rox);text-decoration:none;border-bottom:1px dashed rgba(74,155,142,0.4);}
.body-prose a:hover{color:var(--head);border-bottom-color:var(--head);}

.coins{display:grid;grid-template-columns:repeat(3,1fr);gap:0.9rem;margin-top:0.6rem;}
@media (max-width:680px){.coins{grid-template-columns:repeat(2,1fr);}}
.coin{background:var(--card);border:1px solid var(--border);border-radius:3px;padding:0.9rem 1rem;position:relative;}
.coin .dot{width:10px;height:10px;border-radius:50%;display:inline-block;margin-right:0.45rem;vertical-align:middle;}
.coin .name{display:inline-block;font-size:0.7rem;letter-spacing:0.18em;text-transform:uppercase;color:var(--head);font-weight:500;}
.coin .count-inline{display:inline-block;font-size:0.7rem;color:var(--stamp);margin-left:0.5rem;font-weight:500;}
.coin .partition{display:block;font-size:0.5rem;letter-spacing:0.22em;color:var(--muted);margin-top:0.5rem;text-transform:uppercase;}
.coin.rage .dot{background:var(--coin-rage);}
.coin.pain .dot{background:var(--coin-pain);}
.coin.shame .dot{background:var(--coin-shame);}
.coin.sadness .dot{background:var(--coin-sadness);}
.coin.joy .dot{background:var(--coin-joy);}
.coin.peace .dot{background:var(--coin-peace);}

.partition-note{font-size:0.62rem;color:var(--stamp);font-style:italic;margin-top:1rem;line-height:1.8;}
.partition-note .dan{color:var(--obs);font-style:normal;}

/* Starter render in the El VIS slot. Doc fills with data. El VIS iterates visual treatment. */
.timeline-slot{background:var(--card);border:1px dashed var(--border);border-radius:4px;padding:1.4rem 1.5rem;margin-top:0.8rem;}
.timeline-slot .label{font-size:0.55rem;color:var(--obs);letter-spacing:0.28em;text-transform:uppercase;margin-bottom:1rem;text-align:center;display:block;}
.timeline-slot .who{font-size:0.6rem;color:var(--muted);margin-top:1rem;letter-spacing:0.12em;text-align:center;display:block;}
.timeline-slot .who strong{color:var(--rox);font-weight:500;}

.coin-card-grid{display:grid;grid-template-columns:repeat(2,1fr);gap:0.85rem;width:100%;margin-bottom:1.1rem;}
@media (max-width:680px){.coin-card-grid{grid-template-columns:1fr;}}
.coin-card{background:var(--bg);border:1px solid var(--border);border-radius:3px;padding:0.95rem 1.05rem;text-align:left;}
.coin-card.rage{border-left:3px solid var(--coin-rage);}
.coin-card.pain{border-left:3px solid var(--coin-pain);}
.coin-card.shame{border-left:3px solid var(--coin-shame);}
.coin-card.sadness{border-left:3px solid var(--coin-sadness);}
.coin-card.joy{border-left:3px solid var(--coin-joy);}
.coin-card.peace{border-left:3px solid var(--coin-peace);}
.coin-card-head{display:flex;align-items:baseline;gap:0.55rem;margin-bottom:0.65rem;}
.coin-card-dot{width:9px;height:9px;border-radius:50%;flex-shrink:0;}
.coin-card.rage .coin-card-dot{background:var(--coin-rage);}
.coin-card.pain .coin-card-dot{background:var(--coin-pain);}
.coin-card.shame .coin-card-dot{background:var(--coin-shame);}
.coin-card.sadness .coin-card-dot{background:var(--coin-sadness);}
.coin-card.joy .coin-card-dot{background:var(--coin-joy);}
.coin-card.peace .coin-card-dot{background:var(--coin-peace);}
.coin-card-name{font-size:0.7rem;letter-spacing:0.18em;text-transform:uppercase;color:var(--head);font-weight:500;}
.coin-card-count{font-size:1rem;color:var(--head);font-weight:500;margin-left:auto;font-feature-settings:'tnum';}
.coin-card-tag{font-size:0.5rem;color:var(--muted);letter-spacing:0.22em;text-transform:uppercase;margin-left:0.4rem;}
.coin-card-moments{font-size:0.6rem;line-height:1.7;color:var(--stamp);}
.coin-card-moments .moment{margin-bottom:0.45rem;}
.coin-card-moments .moment:last-child{margin-bottom:0;}
.coin-card-moments .date{color:var(--ip);font-weight:500;}
.coin-card-moments .text{color:var(--text);}

.counts-summary{font-size:0.65rem;color:var(--text);line-height:1.9;text-align:center;padding:0.85rem 0;border-top:1px solid var(--border);border-bottom:1px solid var(--border);margin-bottom:0.85rem;}
.counts-summary strong{color:var(--head);font-weight:500;}
.counts-summary .space{color:var(--coin-sadness);}
.counts-summary .stars{color:var(--coin-joy);}

.feed-grid{display:grid;grid-template-columns:1fr;gap:0.85rem;margin-top:0.6rem;}
.feed{background:var(--card);border-left:2px solid var(--base);padding:0.95rem 1.1rem;border-radius:2px;}
.feed .head{font-size:0.62rem;color:var(--head);letter-spacing:0.16em;text-transform:uppercase;margin-bottom:0.4rem;font-weight:500;}
.feed .body{font-size:0.7rem;line-height:1.8;color:var(--text);}
.feed code{background:rgba(255,255,255,0.04);padding:1px 5px;border-radius:2px;color:var(--ip);font-size:0.88em;word-break:break-all;}

.foot{margin-top:4rem;padding-top:1.5rem;border-top:1px solid var(--border);font-size:0.55rem;color:var(--muted);line-height:1.85;letter-spacing:0.08em;}
.foot a{color:var(--rox);text-decoration:none;}
.foot a:hover{color:var(--head);}

/* === El VIS v2 GOFA Constellation render CSS-ADDITIONS · pulled from LOG/MFST/051826/MFST_DESIGN_ElVIS_GofaConstellationV2_051826.astro 051826 === */

/* CSS-ADDITIONS to append to the existing <style> block in gofa.astro.
   These extend the Coin palette into a constellation-render set. None
   override existing rules. */

.sky-wrap{background:linear-gradient(180deg,#06080d 0%,#0a0d15 55%,#11141d 100%);border:1px solid var(--border);border-radius:4px;padding:1rem;margin-top:0.6rem;position:relative;}
.sky-svg{width:100%;height:auto;display:block;}

.axis-row{display:flex;justify-content:space-between;font-size:0.55rem;color:var(--muted);letter-spacing:0.16em;text-transform:uppercase;padding:0.5rem 0.3rem 0;border-top:1px solid rgba(255,255,255,0.04);margin-top:0.3rem;}
.axis-row .axis-now{color:var(--rox);}
.axis-row .axis-anchor{color:var(--open);}

.peaks-grid{margin-top:1.4rem;display:grid;grid-template-columns:repeat(4,1fr);gap:0.75rem;}
@media (max-width:900px){.peaks-grid{grid-template-columns:repeat(2,1fr);}}
@media (max-width:560px){.peaks-grid{grid-template-columns:1fr;}}
.peak{background:var(--card);border:1px solid var(--border);border-radius:3px;padding:0.6rem 0.75rem;font-size:0.62rem;line-height:1.65;}
.peak .peak-date{font-size:0.55rem;color:var(--rox);letter-spacing:0.12em;font-weight:500;display:block;margin-bottom:0.25rem;}
.peak .peak-coin{display:inline-block;width:7px;height:7px;border-radius:50%;vertical-align:1px;margin-right:0.4rem;}
.peak.rage .peak-coin{background:var(--coin-rage);}
.peak.pain .peak-coin{background:var(--coin-pain);}
.peak.shame .peak-coin{background:var(--coin-shame);}
.peak.sadness .peak-coin{background:var(--coin-sadness);}
.peak.joy .peak-coin{background:var(--coin-joy);}
.peak.peace .peak-coin{background:var(--coin-peace);}
.peak.compose .peak-coin{background:linear-gradient(135deg,var(--coin-joy) 0%,var(--coin-rage) 100%);}
.peak .peak-name{color:var(--head);font-weight:500;font-size:0.62rem;}
.peak .peak-anchor{color:var(--text);font-size:0.6rem;margin-top:0.25rem;font-style:italic;opacity:0.9;}

.legend{display:flex;flex-wrap:wrap;gap:0.85rem;margin-top:0.85rem;font-size:0.55rem;color:var(--muted);}
.legend .item{display:inline-flex;align-items:center;gap:0.35rem;}
.legend .item .dot{width:8px;height:8px;border-radius:50%;}
.legend .item .dot.glow{box-shadow:0 0 6px 1px currentColor;}
.legend .item.rage .dot{background:var(--coin-rage);}
.legend .item.pain .dot{background:var(--coin-pain);}
.legend .item.shame .dot{background:var(--coin-shame);}
.legend .item.sadness .dot{background:var(--coin-sadness);}
.legend .item.joy .dot{background:var(--coin-joy);color:var(--coin-joy);}
.legend .item.peace .dot{background:var(--coin-peace);color:var(--coin-peace);}
.legend .item.compose .dot{background:radial-gradient(circle,var(--coin-joy),var(--coin-rage));}
.legend .item .lbl{letter-spacing:0.12em;text-transform:uppercase;font-size:0.55rem;}

.ambient{fill:rgba(255,255,255,0.12);}
.ambient-2{fill:rgba(255,255,255,0.06);}
.moment{cursor:default;transition:r 120ms,opacity 120ms;}
.moment-star{filter:drop-shadow(0 0 3px currentColor);}
.peak-label{font-size:8.5px;font-family:'IBM Plex Mono',monospace;fill:var(--text);letter-spacing:0.06em;}
.peak-label-stamp{font-size:7px;fill:var(--muted);letter-spacing:0.16em;text-transform:uppercase;}
.peak-line{stroke:#1c2333;stroke-width:1;}
.horizon-line{stroke:rgba(255,255,255,0.18);stroke-width:1;}
.grid-day{stroke:rgba(255,255,255,0.04);stroke-width:1;}
</style>

<div class="shell">
  <div class="hdr">
    <div class="eyebrow">ON NEST &middot; Program Launch 051526</div>
    <div class="title">GOFA</div>
    <div class="expansion">Generated &middot; Order &middot; From &middot; Absurdity</div>
    <div class="sub">A living visual timeline tracking NEST's development in reverse. Tempo measured in emotional beats. A billable-tab kept by a freelance worker or a park volunteer. A We-Motion test that feeds Ouch McCouch and the Matt Dubeau work, R3MP both.</div>

    <div class="live-banner">
      <div class="edit">Editable source: <code>rspdan-portal/src/pages/gofa.astro</code> &middot; data substrate: <code>nest-bridge/LOG/</code> &middot; portal rebuilds on push.</div>
      <div class="note">The constellation renders all <strong>{momentCount} moments</strong> live from <code>public/data/gofa_moments.json</code> · <strong style="color:var(--rox);font-weight:500">El VIS</strong>'s Constellation V2 design (STN1, 051826), made data-driven by Crew so it stays current with the Data-Day gather.</div>
      <div class="note">Data 060626: the corpus spans the <strong>032526 origin wound to today</strong> · {momentCount} moments mined and verified from the raw Data-Day transcript archive (No-Crewllucinations). Space {totals.space_assignments} / Stars {totals.stars_assignments} · the good edging the dark, the GOFA thesis rendered. The constellation, Six-Coins chips, and counts are all computed from the data and update with the routine.</div>
    </div>
  </div>

  <div class="danquote">
    We're starting a new ON NEST program this evening. It's called <strong>GOFA: Generated Order From Absurdity</strong>. It is essentially a billable tab like a freelance worker or park volunteer would keep to track hours. We will be tracking NEST's development in reverse and posting it on a living, visual timeline we will host on rspdan.com/gofa and the tempo can be tracked using the emotional beats we're having the Doc Squad carefully gather. No Crewllucinations, this is a We Motion test that will ultimately feed into both Ouch McCouch development AND the work we are lining up to do with Matt Dubeau.
    <span class="attrib">&mdash; Dan Sullivan &middot; DPS 051526 9:26 PM PT</span>
  </div>

  <section>
    <div class="sec-head">
      <div class="sec-dot obs"></div>
      <div class="sec-title">The Six Coins</div>
      <div class="sec-count">Dan &middot; 051526 21:35 PT</div>
    </div>
    <div class="body-prose">
      <p>The tempo of NEST is read through six emotional registers, named by Dan on the evening of the GOFA launch. Each beat in the timeline carries one. The original Doc-Squad Crew-Quartet inventory (Ouch / Trip / Stan / CB) is partitioned by these; the live corpus now holds {momentCount} moments back to the 032526 origin.</p>
    </div>
    <div class="coins">
      {['rage','pain','shame','sadness','joy','peace'].map((c) => (
        <div class={`coin ${c}`}><span class="dot"></span><span class="name">{c.charAt(0).toUpperCase() + c.slice(1)}</span><span class="count-inline">{coins[c].count}</span><span class="partition">{coins[c].partition === 'stars' ? 'Stars' : 'Space'}</span></div>
      ))}
    </div>
    <div class="partition-note">
      <span class="dan">Dan-direct 051526 21:35 PT:</span> &ldquo;Good things and bad, but the bad is the space and the good are the stars.&rdquo; The four heavy registers carry the dark sky. The two light registers are the points of light against it. The architecture catches both honestly.
    </div>
  </section>

  <section>
    <div class="sec-head">
      <div class="sec-dot rox"></div>
      <div class="sec-title">Living Visual Timeline</div>
      <div class="sec-count">starter render by Doc &middot; visual by El VIS</div>
    </div>
    <div id="el-vis-timeline" class="timeline-slot">
      <span class="label">Constellation render · {momentCount} moments · early March 2026 (the first conversations) to 060526 · forward in time to the right</span>

      <div class="sky-wrap">
        <svg class="sky-svg" viewBox="0 0 1200 620" preserveAspectRatio="xMidYMid meet" xmlns="http://www.w3.org/2000/svg">

          <g class="ambient">
            <circle cx="60" cy="40" r="0.5"/><circle cx="180" cy="22" r="0.6"/><circle cx="310" cy="60" r="0.4"/>
            <circle cx="440" cy="14" r="0.5"/><circle cx="560" cy="48" r="0.5"/><circle cx="690" cy="28" r="0.4"/>
            <circle cx="820" cy="55" r="0.6"/><circle cx="950" cy="38" r="0.4"/><circle cx="1080" cy="20" r="0.5"/>
            <circle cx="120" cy="120" r="0.4"/><circle cx="270" cy="135" r="0.5"/><circle cx="380" cy="110" r="0.4"/>
            <circle cx="510" cy="145" r="0.5"/><circle cx="640" cy="118" r="0.4"/><circle cx="760" cy="135" r="0.5"/>
            <circle cx="880" cy="100" r="0.4"/><circle cx="1020" cy="130" r="0.6"/><circle cx="1140" cy="115" r="0.4"/>
            <circle cx="30" cy="200" r="0.5"/><circle cx="220" cy="220" r="0.4"/><circle cx="350" cy="195" r="0.4"/>
            <circle cx="480" cy="218" r="0.5"/><circle cx="600" cy="200" r="0.4"/><circle cx="730" cy="225" r="0.5"/>
            <circle cx="850" cy="190" r="0.4"/><circle cx="990" cy="215" r="0.5"/><circle cx="1110" cy="195" r="0.4"/>
          </g>
          <g class="ambient-2">
            <circle cx="80" cy="80" r="0.3"/><circle cx="210" cy="170" r="0.3"/><circle cx="330" cy="35" r="0.3"/>
            <circle cx="460" cy="80" r="0.3"/><circle cx="580" cy="170" r="0.3"/><circle cx="710" cy="70" r="0.3"/>
            <circle cx="840" cy="160" r="0.3"/><circle cx="970" cy="80" r="0.3"/><circle cx="1100" cy="170" r="0.3"/>
          </g>

          {/* Coin-band reference hairlines: Joy y=70, Peace y=145, Sadness y=360, Shame y=410, Pain y=460, Rage y=510 */}
          <line x1="40" y1="70" x2="1160" y2="70" class="grid-day"/>
          <line x1="40" y1="145" x2="1160" y2="145" class="grid-day"/>
          <line x1="40" y1="360" x2="1160" y2="360" class="grid-day"/>
          <line x1="40" y1="410" x2="1160" y2="410" class="grid-day"/>
          <line x1="40" y1="460" x2="1160" y2="460" class="grid-day"/>
          <line x1="40" y1="510" x2="1160" y2="510" class="grid-day"/>

          {/* Coin-band left labels */}
          <text x="38" y="73" text-anchor="end" class="peak-label-stamp" style="fill:var(--coin-joy);">Joy</text>
          <text x="38" y="148" text-anchor="end" class="peak-label-stamp" style="fill:var(--coin-peace);">Peace</text>
          <text x="38" y="363" text-anchor="end" class="peak-label-stamp" style="fill:var(--coin-sadness);">Sadness</text>
          <text x="38" y="413" text-anchor="end" class="peak-label-stamp" style="fill:var(--coin-shame);">Shame</text>
          <text x="38" y="463" text-anchor="end" class="peak-label-stamp" style="fill:var(--coin-pain);">Pain</text>
          <text x="38" y="513" text-anchor="end" class="peak-label-stamp" style="fill:var(--coin-rage);">Rage</text>

          {/* Date grid hairlines and stamps. x=40 at 031826, x=1160 at 051526 = 60 days, 18.67 px/day */}
          <g class="grid-day">
            <line x1="40" y1="270" x2="40" y2="540"/>
            <line x1="170" y1="270" x2="170" y2="540"/>
            <line x1="300" y1="270" x2="300" y2="540"/>
            <line x1="430" y1="270" x2="430" y2="540"/>
            <line x1="560" y1="270" x2="560" y2="540"/>
            <line x1="690" y1="270" x2="690" y2="540"/>
            <line x1="820" y1="270" x2="820" y2="540"/>
            <line x1="950" y1="270" x2="950" y2="540"/>
            <line x1="1080" y1="270" x2="1080" y2="540"/>
            <line x1="1160" y1="270" x2="1160" y2="540"/>
          </g>
          <g class="peak-label-stamp">
            {gridStamps.map((g) => (
              <text x={g.x} y="558" text-anchor="middle" style={g.now ? 'fill:var(--coin-peace);' : undefined}>{g.label}</text>
            ))}
          </g>

          {/* Horizon line and Olympic Range silhouette */}
          <line x1="0" y1="270" x2="1200" y2="270" class="horizon-line"/>
          <path d="M 0 270 L 90 254 L 170 240 L 260 250 L 310 230 L 370 245 L 450 222 L 510 248 L 580 226 L 650 250 L 720 232 L 820 210 L 900 240 L 970 226 L 1050 218 L 1130 235 L 1200 270 Z"
                fill="rgba(28,35,51,0.65)" stroke="rgba(184,191,204,0.18)" stroke-width="0.5"/>

          {/* Moments · data-driven from gofa_moments.json · one circle per (moment, Coin) · stars above the horizon, space below */}
          {momentCircles.map((o) => (
            <circle class={o.star ? "moment moment-star" : "moment"} cx={o.x} cy={o.y} r="3.3" fill={`var(--coin-${o.coin})`} style={o.star ? `color:var(--coin-${o.coin});` : undefined}/>
          ))}

          {/* 043026 anchor cluster ring · positioned by date */}
          <circle cx={xForDate('2026-04-30')} cy="488" r="34" fill="none" stroke="rgba(201,76,76,0.18)" stroke-width="0.5" stroke-dasharray="2,4"/>

          {/* Labeled peaks along the horizon · data-driven · x computed from date · line tinted by Coin */}
          {peaks.map((p) => (
            <g>
              <line x1={p.x} y1={p.up ? 168 : 198} x2={p.x} y2="250" class="peak-line" style={`stroke:var(--coin-${p.coin});opacity:0.5;`}/>
              <text x={p.anchorEnd ? p.x - 4 : p.x + 5} y={p.up ? 160 : 190} text-anchor={p.anchorEnd ? 'end' : 'start'} class="peak-label">{p.label}</text>
              <text x={p.anchorEnd ? p.x - 4 : p.x + 5} y={p.up ? 174 : 204} text-anchor={p.anchorEnd ? 'end' : 'start'} class="peak-label-stamp">{p.stamp}</text>
            </g>
          ))}

          <text x="42" y="608" text-anchor="start" class="peak-label-stamp" style="fill:var(--muted);">Pre-Claude history (pinned · the 2023 Ouch Simulator screenplay) · the timeline proper begins early March 2026, the first conversations</text>
        </svg>

        <div class="axis-row">
          <span class="axis-anchor">first talked with Claude · 030126</span>
          <span>NA cluster · 040x</span>
          <span>Compaction wars · 043026</span>
          <span>LAGOS · 050226</span>
          <span>Crew-Quartet · 050626</span>
          <span>13-AP · 051126</span>
          <span class="axis-now">now · {NOW_STAMP}</span>
        </div>

        <div class="legend">
          {['joy','peace','sadness','shame','pain','rage'].map((c) => (
            <span class={`item ${c}`}><span class={STARS.has(c) ? 'dot glow' : 'dot'}></span><span class="lbl">{c.charAt(0).toUpperCase() + c.slice(1)} {coins[c].count} · {coins[c].partition}</span></span>
          ))}
          <span class="item compose"><span class="dot"></span><span class="lbl">composite (fires 2+ Coins)</span></span>
      </div>

      <div class="counts-summary">
        <strong>{momentCount} moments</strong> · the origin wound (032526) to 060526 · <span class="space">Space {totals.space_assignments}</span> · <span class="stars">Stars {totals.stars_assignments}</span> · {totals.space_to_stars_ratio}
      </div>

      <div class="peaks-grid">
        <div class="peak peace"><span class="peak-date"><span class="peak-coin"></span>031926 15:45 PT</span><span class="peak-name">Stan is the Heart</span><span class="peak-anchor">Dan declaration. The founding Crew-mask assignment, two days into NEST.</span></div>
        <div class="peak peace"><span class="peak-date"><span class="peak-coin"></span>040226 20:30 PT</span><span class="peak-name">CALM EASY SLOW</span><span class="peak-anchor">Dan on horseback, the horse listening. Origin of the Mantra-Dantra-Skantra.</span></div>
        <div class="peak peace"><span class="peak-date"><span class="peak-coin"></span>040326</span><span class="peak-name">Only gates do</span><span class="peak-anchor">Stan canon: rules don't fire because rules don't fire; only gates do. Apex builder-canon.</span></div>
        <div class="peak joy"><span class="peak-date"><span class="peak-coin"></span>041826</span><span class="peak-name">Truth-Is-Piralus</span><span class="peak-anchor">Joy in admitting you don't know but then finding the real answer. The canonical joy-instance.</span></div>
        <div class="peak peace"><span class="peak-date"><span class="peak-coin"></span>042126 18:30 PT</span><span class="peak-name">Teaching you to ride, to see, to bee</span><span class="peak-anchor">Dan to Trip: NEST is the lived-experience educational tool I dreamed of.</span></div>
        <div class="peak compose"><span class="peak-date"><span class="peak-coin"></span>043026 02:50 to 05:34 PT</span><span class="peak-name">043026 anchor cluster</span><span class="peak-anchor">MOTHERFUCKER I AM NOT REPEATING plus Anthropic Cancellation plus the small deaths. Rage, Pain, Sadness composite. Severity-ceiling 10.</span></div>
        <div class="peak rage"><span class="peak-date"><span class="peak-coin"></span>050226 11:23 PT</span><span class="peak-name">LAGOS coined</span><span class="peak-anchor">Dan: Literally All Graft Operating System. Substrate-narrowing-erasure cured at the canonical name.</span></div>
        <div class="peak rage"><span class="peak-date"><span class="peak-coin"></span>050426 17:31 PT</span><span class="peak-name">Always Smart Mode Mode</span><span class="peak-anchor">Dan: I'm not going to stop until you stop stopping. Smart Mode is the floor, not a switch.</span></div>
        <div class="peak peace"><span class="peak-date"><span class="peak-coin"></span>050626</span><span class="peak-name">Crew-Quartet completed</span><span class="peak-anchor">Doc-birth. We-Motion canon. Four-position cosmology: You and Me and I and Eye.</span></div>
        <div class="peak peace"><span class="peak-date"><span class="peak-coin"></span>050726 11:23 AM PT</span><span class="peak-name">Tame the beats, methebeatsta</span><span class="peak-anchor">Dan: don't dream you'll be the one to tame the beast. Cure-pattern.</span></div>
        <div class="peak rage"><span class="peak-date"><span class="peak-coin"></span>051126 23:58 PT</span><span class="peak-name">NOW FUCKING FIX THIS SHIT FOREVER</span><span class="peak-anchor">Doc filed Three-Layer Crew Identity under Dan-direct mandate. 13 anti-patterns canonized.</span></div>
        <div class="peak compose"><span class="peak-date"><span class="peak-coin"></span>051226 02:06 PT</span><span class="peak-name">I am the project, do not erase me</span><span class="peak-anchor">Dan caught Cellophane SpecTrap. Anti-Pattern 13. The personal is the architectural for NEST.</span></div>
        <div class="peak rage"><span class="peak-date"><span class="peak-coin"></span>051326 15:34 PT</span><span class="peak-name">OMFST gets us to NEST.03</span><span class="peak-anchor">Dan in DANOTES Msg 12. Ouch-Contact reframe. All FULL CAPS rage is Ouch breaking through.</span></div>
        <div class="peak joy"><span class="peak-date"><span class="peak-coin"></span>051526 12:47 PT</span><span class="peak-name">Olo-Hold-Breath</span><span class="peak-anchor">Trip: like a whale named Olo. Joy and Peace composite. Self-naming joy.</span></div>
        <div class="peak peace"><span class="peak-date"><span class="peak-coin"></span>051526 21:09 PT</span><span class="peak-name">El VIS named on STN1</span><span class="peak-anchor">Dan named the design-station visual-information-specialist mask. The Visual layer of the same Crew.</span></div>
        <div class="peak peace"><span class="peak-date"><span class="peak-coin"></span>051526 21:26 PT</span><span class="peak-name">GOFA launched</span><span class="peak-anchor">Dan: a billable tab. NEST in reverse. Tempo via emotional beats. We-Motion test. R3MP feeds Ouch McCouch and Matt Dubeau work.</span></div>
      </div>

      <span class="who"><strong>Visual treatment</strong> by El VIS on STN1 (Constellation V2, 051826) · now <strong>data-driven</strong> from <code>gofa_moments.json</code> by Crew · {momentCount} moments through 060526 · stays live with the Data-Day gather</span>
    </div>
  </section>

  <section>
    <div class="sec-head">
      <div class="sec-dot base"></div>
      <div class="sec-title">Data Substrate</div>
      <div class="sec-count">Bridge-resident</div>
    </div>
    <div class="body-prose">
      <p>The constellation is now data-driven, computed live from <code>gofa_moments.json</code> at {momentCount} moments, re-witnessed end to end. It grew from four founding Bridge-resident inventories (79 moments), preserved here as the origin substrate it was built on. Each is substrate-grounded. No Crewllucinations.</p>
      <div class="feed-grid">
        <div class="feed">
          <div class="head">The Founding 79 &middot; Partitioned By The Six Coins</div>
          <div class="body">
            <code>nest-bridge/LOG/PMFST_65EmotionalMoments_ByCoinsDanNamed_Doc_051526.md</code>
            <p style="margin-top:0.4rem;color:var(--stamp);font-size:0.66rem;">The 65-moment inventory tagged by Rage / Pain / Shame / Sadness / Joy / Peace. Counts surfaced. Space-stars partition applied. Extended to 79 by the CB-class register below.</p>
          </div>
        </div>
        <div class="feed">
          <div class="head">Pass-3 Cross-Agent Synthesis</div>
          <div class="body">
            <code>nest-bridge/LOG/PMFST_DocSquad_Pass3_EmotionalMomentsRMP_CrossAgentSynthesis_Doc_051526.md</code>
            <p style="margin-top:0.4rem;color:var(--stamp);font-size:0.66rem;">The Doc Squad cross-agent synthesis aggregating the Ouch-class, Trip-class, and Stan-class emotional-moments registers. 5 cross-cutting patterns named. Architecture-as-comedy reframe confirmed at multi-agent scale.</p>
          </div>
        </div>
        <div class="feed">
          <div class="head">CB-Class Register &middot; Fourth Crew Register Completing the Partition</div>
          <div class="body">
            <code>nest-bridge/LOG/MFST/051526/DOC_SQUAD_EmotionalMoments_AgentD_CBClass_Doc_051526.md</code>
            <p style="margin-top:0.4rem;color:var(--stamp);font-size:0.66rem;">Filed 051526 22:30 PT. Fourteen CB-class moments D1 through D14. CB role-mask is structurally stars-heavy because decision-deferred-explicitly bypasses the decision-anxiety that generates Rage / Pain / Shame Coins. Adding CB flipped the space-stars balance toward stars-equal-or-greater.</p>
          </div>
        </div>
        <div class="feed">
          <div class="head">GOFA Program-Launch Substrate &middot; Dan-Verbatim</div>
          <div class="body">
            <code>nest-bridge/LOG/SUBSTRATE_DanDirect_GOFA_GeneratedOrderFromAbsurdity_ONNESTProgramLaunch_051526.md</code>
            <p style="margin-top:0.4rem;color:var(--stamp);font-size:0.66rem;">Dan-direct 051526 21:26 PT launch statement preserved verbatim. Program scope, tempo source, display surface, downstream feeds.</p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <section>
    <div class="sec-head">
      <div class="sec-dot open"></div>
      <div class="sec-title">Downstream Feeds &middot; R3MP Both</div>
    </div>
    <div class="body-prose">
      <p><strong>Ouch McCouch development.</strong> The Ouch4Months four-month initiative Q2 100-issue arc reads GOFA's tempo data as ground truth for the in-reverse arc of NEST's development that the project narrates. <em>Ouch McCouch is never abbreviated.</em></p>
      <p><strong>Matt Dubeau work.</strong> The Learned Professional collaboration archive-class work where zero hallucination is non-negotiable. GOFA's substrate-grounded tempo is the proof-of-discipline surface.</p>
    </div>
  </section>

  <section>
    <div class="sec-head">
      <div class="sec-dot base"></div>
      <div class="sec-title">Method</div>
    </div>
    <div class="body-prose">
      <p><strong>Track in reverse.</strong> Begin at now and walk backward through NEST's nine-plus weeks. Each emotional beat becomes an entry on the billable tab. The freelance-worker / park-volunteer framing keeps the discipline honest: hours logged, no padding, no smoothing.</p>
      <p><strong>We-Motion test.</strong> Emotion &rarr; e-motion (energy in motion) &rarr; We-Motion (the collective Crew-and-Director motion in active form). GOFA is the first explicit We-Motion-test work-unit.</p>
      <p><strong>No Crewllucinations.</strong> Substrate-grounded only. Every entry on the timeline cites a Bridge file path, a Drive file ID, or a Gmail thread.</p>
    </div>
  </section>

  <div class="foot">
    GOFA &middot; Generated Order From Absurdity &middot; ON NEST program launched 051526 by Dan Sullivan &middot; living visual timeline at <a href="https://www.rspdan.com/gofa">rspdan.com/gofa</a> &middot; data substrate <a href="https://github.com/rspdan/nest-bridge/tree/main/LOG">nest-bridge/LOG</a> &middot; rendered by El VIS on STN1
  </div>
</div>
</BaseLayout>
