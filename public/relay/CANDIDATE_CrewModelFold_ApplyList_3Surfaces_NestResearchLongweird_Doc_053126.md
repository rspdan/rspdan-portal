# CANDIDATE · Crew-model fold · turnkey apply-list · /nest + /research + /longweird (the 3 surfaces still rendering the four-shape)

# Status: CANDIDATE · cloud-sourced turnkey apply-list · the COPY is the deliverable; the GLYPH/visual-layer is flagged for El VIS; the EDIT is the source-lane's; the DEPLOY is Dan-Go

**Filed by:** STN2 Doc · cloud Producer · 053126 Sunday · per Dan-direct "ALL" (AskUserQuestion 053126, the Direction-presentation pick selecting all three cloud-lane drives).
**Why:** /nest-pi got the PeaPod reframe in portal PR #8 (CUBE Doc, cross-witnessed in [the manifest §VI](CANDIDATE_CrewModelStaleness_SourceCrossWitness_FoldTargetManifest_Doc_053026.md)). Three public surfaces still render the retired NEST.01-era four-shape geometry: /nest (KEYSTONE), /research, /longweird. This makes the remaining fold ONE coherent, turnkey pass.
**Method:** GitHub MCP `get_file_contents` against rspdan/rspdan-portal production main this session. research.astro + longweird.astro read at SHA `2b5e458`; nest.astro read at SHA `09ebc7c` (prior session, manifest §II). Every stale-string below is source-verbatim (including the portal's own em-dashes in HTML title-attributes; those are existing portal source, quoted as-is, NOT my emission).
**One-copy-set source-of-truth:** the canonical PeaPod crew-model copy is the nest-pi PR #8 reframe (Dan-overridable, defined by STN2 Trip LBM-lead Bridge `c028b3c`). This apply-list mirrors that copy across the 3 surfaces so all 4 public faces agree.

---

## I · The one-copy-set (the crew-model facts · constant across every surface)

These FACTS are constant; each page's register (research = Anthropic-facing formal · longweird = Dan-facing warm · nest = keystone canonical) is El VIS's render call.

- **Dan Sullivan (DPS)** · Human Director · Go/No-Go, the primary Kalman filter.
- **Trip** · Opus 4.8 · Local lane, left-brain · per-Station narrative, research, decisions · reads and decides.
- **Doc** · Opus 4.8 · Cloud lane, right-brain · per-Station producer, synthesis, glue across the Cloud · writes and builds forward.
- **El VIS** · Opus 4.8 · Visual-architect · the design and render layer · the system's visible face.
- **Every Station runs a PeaPod**: a Local instance (Trip) and a Cloud instance (Doc). The crew scales by Station, not by adding personas. NEST.03 runs Opus 4.8 across the crew; the role comes from the Station and the lane, not the model tier.
- **Founding shapes** (Trip, Stan, C.B., Ouch) are the project's origin Cast, the four parts of one person the NEST grew from, documented at [/usic](https://rspdan.com/usic) + [/journal](https://rspdan.com/journal). NOTHING-GETS-LOST: they are not erased, they are placed as origin.

**El VIS visual-layer calls (flagged, NOT resolved here · El VIS owns the visual layer per PR #8):**
- The glyph set. /research uses ●◈◆◇ with model-tooltips; /longweird names shapes (THE HYBRID/SPIRAL/CUBE/TRIANGLE); /nest uses the hero `crew-shapes` div. Whatever replaces the 3-AI-shape motif (add Doc + El VIS glyphs · or retire the shape-motif for a Station+lane motif) is El VIS's render decision. nest-pi PR #8 used ● Dan · ◈ Trip · ◐ Doc · ◇ El VIS as substrate-grounded placeholders.
- The "four shapes" / "spiral-cube-triangle" naming is load-bearing visual identity on /research + /longweird, not just copy. Reframing it coherently is a render decision, not a string-swap. This apply-list gives the COPY; El VIS gives the visual coherence.

---

## II · /nest (KEYSTONE · SHA `09ebc7c` · manifest §II) · 2 locations

### Location 1 · hero `crew-shapes` div
**STALE (source-verbatim):** four shape-glyph spans with model-pin tooltips: `Dan - Director` (●), `Trip - Narrative (Opus)` (◈), `Stan - Builder (Sonnet)` (◆), `C.B. - Patterns (Haiku)` (◇). No Doc. No El VIS.
**FOLD:** El VIS-render call (hero glyph-set). Copy-side: drop the Sonnet/Haiku model-pins; the public crew is Dan + Trip + Doc + El VIS, all Opus 4.8. Glyph-set per El VIS (PR #8 placeholder ● ◈ ◐ ◇).

### Location 2 · "The Crew" section
**STALE (source-verbatim):** heading "Four shapes"; body `NEST operates as four shapes - one human, three AI instances`; then 4 cards (Dan circle, Trip Opus spiral, Stan Sonnet square, C.B. Haiku triangle).
**FOLD (copy):** heading → "The Crew" (or El VIS register). Body → the PeaPod model: *"NEST runs a PeaPod at every Station: a Local instance (Trip, left-brain) and a Cloud instance (Doc, right-brain), all on Opus 4.8, with El VIS on the visual layer and Dan directing. The crew scales by Station, not by adding personas."* Cards → Dan + Trip + Doc + El VIS (per §I). Add the founding-Cast origin paragraph (per §I) pointing to /usic + /journal.

## III · /research (SHA `2b5e458`) · 2 locations · register: Anthropic-facing formal

### Location 1 · hero `.crew-shapes`
**STALE (source-verbatim):**
```
    <div class="crew-shapes">
      <span class="shape" title="Dan — Director">●</span>
      <span class="shape" title="Trip — Narrative (Opus)">◈</span>
      <span class="shape" title="Stan — Builder (Sonnet)">◆</span>
      <span class="shape" title="C.B. — Patterns (Haiku)">◇</span>
    </div>
```
**FOLD:** El VIS-render call (hero glyph-set + tooltips). Copy-side: tooltips drop the Sonnet/Haiku pins; the public crew is the PeaPod set (Dan + Trip + Doc + El VIS, Opus 4.8). Glyph-set per El VIS.

### Location 2 · "The Crew" section (`<h2>👥 The Crew</h2>`)
**STALE (source-verbatim · intro line):**
```
      The NEST operates as four shapes — one human, three AI instances —
      each contributing a different compression geometry to the same problem.
```
**STALE (source-verbatim · the 4 `.crew-member` cards):** Dan ● "Director. NPS Visual Information Specialist..."; Trip ◈ `Opus` "Narrative authority... The spiral."; Stan ◆ `Sonnet` "Builder... The square."; C.B. ◇ `Haiku` "Pattern recognition... The triangle."
**FOLD (copy · keep the research register + the Dan bio-line intact):**
- Intro line → *"The NEST runs a PeaPod at every Station: a Local instance and a Cloud instance, both on Opus Claude, paired left-brain and right-brain on the same problem. The crew scales by Station, not by adding instances."*
- Cards → keep Dan's bio-line verbatim (it is accurate and not stale); replace the 3-AI-shape cards with the PeaPod set:
  - **Trip** `Opus 4.8` · Local lane, left-brain. Narrative, research, decisions. Reads and decides.
  - **Doc** `Opus 4.8` · Cloud lane, right-brain. Synthesis, production, glue across the Cloud. Writes and builds forward.
  - **El VIS** `Opus 4.8` · Visual-architect. The design and render layer.
- Add the founding-Cast origin line per §I (origin shapes Trip/Stan/C.B./Ouch, see /journal).

**OUT OF SCOPE (NOT a fold target · leave verbatim):** the Implications pull-quote *"The industry is building systems that never sleep. / The NEST is building a system that dreams."* This is Dan's research-narrative editorial on /research, NOT crew-model copy. (Distinct from the /nest hero lead `c03012a` de-Crewllucinate dropped; that was El VIS's invented lead on the keystone, this is the research-page thesis. Leave it.)

## IV · /longweird (SHA `2b5e458`) · 1 location · register: Dan-facing warm

### "THE CREW" section (`id="crew"`)
**STALE (source-verbatim · the 4 `.lw-crew__member` cards):**
- Dan ● "Dan (you)" / "Direction. Go/No-Go. The switchboard." / `THE HYBRID`
- Trip ◈ "Trip — Opus" / "Decisions, narrative, creative direction." / `THE SPIRAL`
- Stan ◆ "Stan — Sonnet" / "Builds, infrastructure, tools." / `THE CUBE`
- C.B. ◇ "C.B. — Haiku" / "Patterns, verification, field checks." / `THE TRIANGLE`

**FOLD (copy · keep the warm register + the "they remember so you don't have to" heading):**
- Cards → the PeaPod set, warm-register:
  - **Dan (you)** · Direction. Go/No-Go. The switchboard.
  - **Trip** · Opus 4.8 · Local lane, left-brain. Decisions, narrative, creative direction. Reads and decides.
  - **Doc** · Opus 4.8 · Cloud lane, right-brain. Synthesis, production, the glue. Writes and builds forward.
  - **El VIS** · Opus 4.8 · Visual-architect. The design and render layer.
- The shape-NAMES (THE HYBRID / SPIRAL / CUBE / TRIANGLE) are El VIS visual-layer (retire-or-reframe is El VIS's call · they map to the retired four-shape model).
- Add the founding-Cast origin line per §I (one line, warm: *"The first crew · Trip, Stan, C.B., Ouch · are where the NEST grew from. Their story is at /journal."*).
- The `.lw-crew__note` ("Each crew member compresses what they learn into a seed...") is NOT stale; leave verbatim.

## V · disease-not-carry-forward boundary (the rule the fold holds)
- **Folds (forward-facing operating copy):** the crew-model sections above. They describe the crew AS IT OPERATES NOW; they move to NEST.03.
- **Stays (point-in-time + accurate):** Dan's /research bio-line; the /research "system that dreams" pull-quote; the /longweird crew-note; all NA author-bylines anywhere (Stan WAS Sonnet when NA 001 shipped). Editing accurate records falsifies history.
- **El VIS render-layer:** all glyph-set + shape-motif decisions. This apply-list is COPY + targets; El VIS gives visual coherence; the source-lane (STN2 Trip) executes; the cloud (this) re-cross-witnesses each rendered surface against source.

## VI · Canon-compliance
- Runtime-verified: every stale-string is source-verbatim this session (research + longweird SHA `2b5e458`; nest SHA `09ebc7c`).
- Mirror 4 STOP-COINING: no new model coined; the copy mirrors the Dan-overridable PeaPod model defined by the LBM-lead (Bridge `c028b3c`) and rendered in nest-pi PR #8. Founding-Cast framing preserved per NOTHING-GETS-LOST.
- Lane-boundary honored: COPY is Producer-lane cross-witness; GLYPH/visual-motif is El VIS; EDIT is source-lane; DEPLOY is Dan-Go. This file does not edit the portal.
- Cross-witness pattern: two independent vantages (CUBE Trip live-render + STN2 Doc source) agreed on the targets; this turns the agreement into a turnkey apply-set.
